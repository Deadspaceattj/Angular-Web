Skip to content
Search or jump to…
Pull requests
Issues
Marketplace
Explore
 
@Deadspaceattj 
loiane
/
my-first-static-app
Public
generated from staticwebdev/angular-basic
Code
Issues
Pull requests
18
Actions
Projects
Security
Insights
my-first-static-app/.github/workflows/azure-static-web-apps-red-cliff-043633f10.yml
@loiane
loiane Update azure-static-web-apps-red-cliff-043633f10.yml
Latest commit d529827 on May 11, 2020
 History
 2 contributors
@loiane@invalid-email-address
42 lines (39 sloc)  1.58 KB

name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
    - master
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
    - master

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
    - uses: actions/checkout@v1
    - name: Build And Deploy
      id: builddeploy
      uses: Azure/static-web-apps-deploy@v0.0.1-preview
      with:
        azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_RED_CLIFF_043633F10 }}
        repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
        action: 'upload'
        ###### Repository/Build Configurations - These values can be configured to match you app requirements. ######
        app_location: '/src' # App source code path
        api_location: '' # Api source code path - optional
        app_artifact_location: 'dist/DunamiX-Angular-Web' # Built app content directory - optional
        ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
    - name: Close Pull Request
      id: closepullrequest
      uses: Azure/static-web-apps-deploy@v0.0.1-preview
      with:
        azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_ASHY_STONE_049CE7E10 }}
        action: 'close'
